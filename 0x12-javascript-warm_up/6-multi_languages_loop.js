#!/usr/bin/node
//Print a multiline using array of String and a loop

const languages = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (const language of languagues){
	console.log(language);
}
